# 项目评审与风险管理报告 — Regression Analysis

> 评审日期：2026-05-25  
> 评审类型：项目启动阶段预审  
> 项目状态：概念阶段（仅 README）

---

## 1. 项目可行性评估

### 1.1 技术可行性

| 维度 | 结论 |
|------|------|
| 核心算法 | **高可行性**。statsmodels / scikit-learn / pingouin 等 Python 库提供了从 OLS 到混合效应模型的完整支持。R 语言生态（lm, lme4, broom）亦可作为后端备选。 |
| Web 前端 | **高可行性**。React + Plotly/D3.js 或 Vue + ECharts 均可实现交互式表格、系数图和诊断图。Streamlit / Dash 更可快速搭建 MVP。 |
| 数据上传与处理 | **高可行性**。pandas 处理 CSV/Excel/Stata/SPSS 格式已是成熟方案，支持 10 万行以内数据的内存操作。 |
| 导出功能 | **高可行性**。报告导出可用 Quarto / markdown + pandoc / jsPDF / exceljs。表格导出原生支持 CSV/LaTeX/HTML。 |
| 图表生成 | **高可行性**。matplotlib / seaborn / plotly 覆盖残差图、QQ 图、系数图、边际效应图等。 |

### 1.2 总体判断

**技术上完全可行**，核心难点不在于"能否实现"，而在于"能否在易用性和统计正确性之间取得平衡"。项目应优先聚焦**应用社会科学研究者**这一细分群体，而非追求覆盖所有统计模型。

### 1.3 关键风险点

- 统计模型的边界情况处理（奇异矩阵、完美共线性、分离问题）容易忽略
- 大文件上传（>100MB）的浏览器内存限制
- 回归输出的统计解读辅助（p 值、置信区间、效应量）需要领域知识驱动 UI 设计
- 如果选择 Web 架构，离线使用能力受限

---

## 2. 风险评估矩阵

### 风险列表

| 编号 | 风险描述 | 可能性 | 影响程度 | 缓解措施 |
|------|---------|--------|---------|---------|
| R01 | **统计模型输出不准确**：在边界条件下（如小样本、高共线性、奇异矩阵）返回错误或误导性结果 | 中 | 高 | 对每个回归模型实现输入校验层；使用 statsmodels 的稳健标准误作为默认选项；编写 50+ 边界测试用例 |
| R02 | **大文件/大数据导致前端崩溃**：用户上传超过 50MB 或 20 万行的数据集时浏览器 OOM | 中 | 高 | 前端实施行数/大小硬限制并提前警告；后端分页处理；初期限制 5 万行/20MB |
| R03 | **数据隐私和安全泄露**：用户上传的数据包含敏感信息，Web 版本可能将数据经服务器传输 | 高 | 高 | 优先支持纯本地处理架构（如 Electron 或纯前端 WASM）；Web 版本必须声明数据不上传服务器；提供数据匿名化提示 |
| R04 | **用户误用统计方法**：用户对变量类型理解错误（如将分类变量当作连续变量使用）导致无效结果 | 高 | 中 | 建立变量类型自动检测 + 可视化标识系统；对可疑变量类型给出警告而非静默执行 |
| R05 | **导出结果质量不达标**：LaTeX/Word/HTML 导出中表格格式错乱、图表分辨率低或字体缺失 | 中 | 中 | 以 Quarto 或 markdown 为核心渲染管线；使用矢量图格式（SVG/PDF）导出图表；建立导出一致性测试 |
| R06 | **浏览器兼容性问题**：统计计算在 Web Worker 中执行时不同浏览器表现不一致 | 低 | 中 | 初期仅支持 Chrome/Firefox/Edge 最新两个大版本；使用 core-js polyfill；关键计算在主线程降级执行 |
| R07 | **与现有工具功能重叠、差异化不足**：做成了"另一个功能更少的 jamovi" | 中 | 高 | 前期完成竞品分析并明确定位差异化方向；聚焦单一痛点场景（如论文报告自动化生成）而非泛化统计平台 |
| R08 | **项目范围蔓延**：用户不断要求增加新模型类型（逻辑回归、多项式回归、多层次模型等），导致核心体验打磨不足 | 高 | 中 | 采用"核心完备 + 扩展可选"架构：v1.0 仅支持线性回归和基础诊断；后续版本增强。每个新模型需经 80% 测试覆盖率审查 |
| R09 | **依赖库版本冲突或停更**：statsmodels、pandas 等上游库的大版本变更导致回归结果不一致 | 低 | 高 | 锁定依赖版本号；每次重大升级前后运行回归结果快照对比测试；CI/CD 中固化回归基准输出 |
| R10 | **文档和教程不足**：目标用户（社科研究者）需要非技术化的使用说明，若缺失则难以采用 | 中 | 中 | 每个功能模块配一个"社科论文案例"作为教程；提供示例数据集（如 mtcars、iris 的社科类比版本）；集成内联帮助提示 |
| R11 | **缺少离线使用能力**：如果采用纯 Web 架构，用户在无网络或教室/会议场景下无法使用 | 中 | 中 | 优先考虑 PWA 架构或 Electron 桌面应用；服务 Worker 缓存核心计算库 WASM |
| R12 | **无版本控制/可复现性支持**：用户无法记录分析步骤，导致研究复现困难 | 中 | 低 | 在 v1.1+ 增加分析"快照"功能：保存变量选择和参数设置的 JSON 文件，支持一键重跑 |

### 风险热力图

```
影响：高         R03 R01         R02 R08
      中         R04 R05         R06 R07 R10 R11
      低         R12             R09
               低      中       高
                      可能性
```

**需优先关注**：R03（隐私）、R01（正确性）、R02（性能）、R08（范围管理）

---

## 3. 质量标准和验收标准

### 3.1 核心功能模块验收标准

#### 模块 A：数据上传与预览

| 验收项 | 标准 |
|--------|------|
| 支持格式 | CSV（UTF-8/GBK）、Excel（.xlsx/.xls）、Stata（.dta）、SPSS（.sav） |
| 文件大小 | ≤20MB 的单文件正常处理，超出时给出明确提示 |
| 缺失值处理 | 自动检测并标亮缺失单元格；提供三种处理选项（删除整行、均值填补、不处理） |
| 变量预览 | 显示变量名、类型（连续/分类/序数）、缺失率、唯一值计数、基本描述统计 |
| 编码检测 | 上传 CSV 时自动检测 UTF-8/GBK/ASCII，检测失败时允许用户手动选择 |

#### 模块 B：变量选择与模型配置

| 验收项 | 标准 |
|--------|------|
| 因变量选择 | 仅允许连续变量作为因变量（二元/多分类变量给出警示） |
| 自变量支持 | 连续变量自动作为数值预测变量；分类变量自动生成虚拟变量（dummy coding） |
| 交互项 | 支持通过 UI 拖拽或勾选创建交互项，交互项在结果中有明确标识 |
| 模型选项 | 常数项（截距）开关；稳健标准误（HC1/HC2/HC3）；VIF 共线性诊断开关 |
| 变量变换 | 支持对数变换、标准化（z-score）、中心化，变换后在变量列表中有后缀标识 |

#### 模块 C：回归结果输出

| 验收项 | 标准 |
|--------|------|
| 系数表 | 展示：变量名、系数（B）、标准误、t 值、p 值、95% CI、标准化系数（Beta）；显著系数加星号标识 |
| 模型拟合统计 | R²、调整 R²、F 统计量及其 p 值、AIC/BIC、RMSE、残差自由度 |
| 诊断统计 | Durbin-Watson（自相关）、VIF（共线性）、Cook's distance 异常值标识 |
| 方差分析表 | 回归 + 残差的 SS/df/MS/F 分解表 |
| 统计警示 | 当 VIF > 10、Cook's distance > 4/n、DW < 1 或 > 3 时自动标红警示 |

#### 模块 D：可视化诊断

| 验收项 | 标准 |
|--------|------|
| 残差图 | 残差 vs 拟合值散点图（含 loess 平滑曲线） |
| QQ 图 | 标准化残差的正态 QQ 图 |
| 尺度-位置图 | 标准化残差平方根 vs 拟合值（检验等方差性） |
| 系数图 | 系数估计值及其 95% CI 的森林图 |
| 图形交互 | 支持悬停显示数值、缩放、保存为 PNG/SVG |

#### 模块 E：导出功能

| 验收项 | 标准 |
|--------|------|
| 表格导出 | CSV（原始数据）、LaTeX（booktabs 格式）、HTML、Markdown |
| 图表导出 | PNG（300 DPI）、SVG（可编辑矢量）、PDF |
| 报告导出 | 完整分析报告整合为 HTML 或 PDF，包含：模型描述 → 描述统计表 → 回归表 → 诊断图 → 解读摘要 |
| 复现包导出 | 输出一个包含原始数据 + 参数配置 JSON + 分析脚本（Python/R）的 ZIP 包 |
| 导出一致性 | 同一数据集 + 同一参数设置每次导出结果完全一致（确定性） |

### 3.2 代码质量标准

| 类别 | 标准 |
|------|------|
| 测试覆盖率 | 统计计算核心模块行覆盖率 ≥ 90%；UI 组件覆盖率 ≥ 60% |
| 回归基准测试 | 每个回归模型有对应的基准输出（已知数据集 + 已知结果），CI 中自动对比 |
| 静态分析 | Python: ruff + mypy (strict mode)；前端: ESLint + TypeScript strict |
| 性能预算 | 10 万行 × 20 变量的 OLS 回归，从点击"运行"到结果展示 ≤3 秒 |
| 文档 | 每个公开函数/方法有 docstring；用户界面有内联帮助文本；API 变更需更新 changelog |

### 3.3 用户体验标准

| 类别 | 标准 |
|------|------|
| 新手引导 | 首次使用时提供 3-5 步交互式引导；提供 2 个示例数据集的内置加载 |
| 错误提示 | 统计错误（如奇异矩阵）以自然语言解释而非堆栈跟踪呈现；提供修正建议 |
| 响应时间 | 每个操作的视觉反馈 ≤200ms；长时间计算显示进度条或预估时间 |
| 布局适配 | 支持 1366×768 及以上分辨率；支持缩放 100%-150% 无布局错乱 |
| 无障碍 | 关键操作支持键盘导航；图表有 alt 文本描述；颜色方案支持色盲友好模式 |

---

## 4. 里程碑建议

### 总体策略：4 个阶段，12-16 周

```
Phase 1  Phase 2   Phase 3   Phase 4
[  POC  ] [ MVP ] [  Beta ] [  v1.0  ]
  2-3周     4周       4周       2-3周
```

---

### Phase 1：概念验证（POC）— 第 1-3 周

**目标**：验证核心技术可行性，消除最大技术风险。

**交付物**：
- 基于 Python 的后端回归引擎原型（支持 OLS + 诊断 + 4 种诊断图）
- 基于 Streamlit 或 Jupyter Widget 的最小可交互原型
- 可运行的端到端 demo（上传 CSV → 选变量 → 看结果）
- 回归基准测试套件（5 个经典数据集，与 R `lm()` 精确对比）

**退出标准**：
- 3 个经典数据集的回归系数与 R 输出差异 < 1e-10
- 端到端 demo 可在 10 分钟内演示给非技术观众
- 所有"高可能性 + 高影响"风险（R01, R02, R03）至少完成初步验证
- 决策：继续 MVP / 否决项目

---

### Phase 2：最小可行产品（MVP）— 第 4-7 周

**目标**：构建可用、可测试的核心功能集，获得早期用户反馈。

**交付物**：
- 完整数据导入模块（CSV/Excel/Stata/SPSS） + 预览表格
- 变量选择 UI + 模型配置面板
- 回归结果展示（系数表 + 拟合统计 + 诊断指标）
- 4 种标准诊断图（残差图、QQ 图、尺度-位置图、系数图）
- CSV/Markdown 导出
- 内联帮助系统（变量类型建议、统计警示）
- 部署至测试环境（或发布桌面测试版）

**退出标准**：
- 内部测试通过 20 个回归场景，无不正确输出
- 3 名外部试用者（社科研究者）完成端到端任务且无关键功能阻塞
- 核心计算模块测试覆盖率 ≥ 85%
- 用户测试反馈：核心工作流（导入 → 建模 → 导出）满意度 ≥ 3.5/5
- 加载公开数据集（n=500, k=10）的回归计算时间 ≤ 1s

---

### Phase 3：Beta 测试与完善 — 第 8-11 周

**目标**：基于真实用户反馈打磨体验，补全高级功能。

**交付物**：
- 交互项创建 UI
- 稳健标准误选项
- 方差分析表
- 完整导出功能（LaTeX/HTML/PDF 表格 + SVG/PNG 图表 + 完整报告）
- 分析复现包导出
- 用户引导和示例数据集
- Bug 修复和性能优化
- Beta 测试版 + 反馈通道

**退出标准**：
- Beta 测试者 ≥ 10 人，每人完成 ≥ 3 个分析任务
- 无 P0（数据丢失/错误结果）Bug
- P1（核心功能受阻）Bug 关闭率 ≥ 90%
- 用户满意度 ≥ 4.0/5
- 测试覆盖率 ≥ 90%（核心模块）
- 文档覆盖率 100%（所有 UI 元素有帮助文本）

---

### Phase 4：正式发布（v1.0）— 第 12-14 周

**目标**：打磨、包装和发布。

**交付物**：
- 最终版应用（Web/桌面可执行文件）
- 用户手册（含 3 个完整案例）
- 项目官网或 README 文档更新
- 已知问题清单（Known Issues）
- v1.0 版本发布

**退出标准**：
- 所有 P1 及以下 Bug 已知且记录，P0/P1 为零
- 用户手册通过 1 名非技术人员的独自阅读测试（无需额外解释即可完成示例任务）
- 导出质量：LaTeX 表格可直接用于期刊投稿，无需手动调整
- 性能：10万行 × 20 变量 → 3s 内完成
- 代码无已知安全漏洞（依赖审计通过）

---

### 可选：Phase 5（v1.1+）— 第 15 周以后

- 多层次模型（混合效应模型）
- 逻辑回归和多项式回归
- 贝叶斯回归（基于 PyMC / Bambi）
- 分析快照（复现性支持的 JSON 配置保存/加载）
- 插件系统

---

## 5. 竞品分析

### 5.1 竞品概览

| 工具 | 类型 | 费用 | 核心优势 | 核心劣势 | 目标用户 |
|------|------|------|---------|---------|---------|
| SPSS | 桌面 GUI | 付费（昂贵） | 点选即用、社科标配 | 闭源、昂贵、模型老旧、图表丑陋 | 社科研究者 |
| Stata | 桌面 GUI+命令 | 付费（昂贵） | 命令可复现、面板数据强 | 学习曲线陡、图形一般、价格高 | 计量经济学 |
| RStudio (Posit) | IDE | 免费 | 最强统计生态、可复现、Quarto | 需编程、学习曲线最陡 | 统计编程用户 |
| jamovi | 桌面 GUI | 免费 | 点选即用、R 后端强大、SPSS 替代 | 界面略显拥挤、高级模型需安装模块 | SPSS 迁移用户 |
| JASP | 桌面 GUI | 免费 | 贝叶斯友好、输出美观 | 模型覆盖面有限、自由度不如 R | 心理学/贝叶斯用户 |
| Python (statsmodels) | 代码库 | 免费 | 灵活、可嵌入、MLOps 兼容 | 需编程、无 GUI | 数据科学家 |
| Gretl | 桌面 GUI | 免费 | 轻量、时间序列强 | UI 过时、生态小 | 经济学教学 |
| Orange | 可视化编程 | 免费 | 拖拽式工作流 | 回归功能较浅 | 教学/探索 |

### 5.2 市场空白与差异化机会

#### 空白 1：从"结果"到"论文就绪"
现有工具的输出都是"半成品"——SPSS 表格需要手动调整格式，jamovi/JASP 的输出在论文提交前仍需截图或重新排版。**我们的工具应该直接产出投稿级表格和图表**：

- LaTeX 表格直接符合 APA7 或期刊模板规范
- 图表 300 DPI + 期刊要求的字体/字号预设
- 一键生成方法章节的描述文本（"本研究报告了 OLS 回归结果，F(3, 96) = 12.45, p < .001, R² = .28"）

#### 空白 2：从"统计工具"到"研究伴侣"
现有工具管理分析过程但不管理分析"故事"：

- 内置假设检验的"检查清单"——用户在选变量后得到假设违反警告和具体修正建议
- 自动生成回归结果的文字解读草稿（适合放入论文"结果"章节）
- 版本比较——两次分析结果的差异高亮

#### 空白 3：中文研究者的专用体验
英文工具在中文环境有显著摩擦：

- 中文 CSV 的编码检测（UTF-8 vs GBK）是中文用户的刚需
- 变量名/值标签的中文字段友好显示
- 帮助文档和错误提示的中文本地化
- 输出中符合中文论文惯例的统计报告格式

#### 空白 4：轻量级在线协作与复现
介于"单机 SPSS"和"完整 R 项目"之间：

- 分析参数存为一个 JSON 文件 → 别人打开自动复现分析
- 支持导出"分析环境包"（数据子集 + 配置 + 结果）
- 未来可能：参数配置的 URL 分享，接收方一键加载

### 5.3 差异化定位建议

```
不做：  "另一个统计软件"（与 jamovi/JASP 正面竞争）
要做：  "从数据到投稿的一站式回归报告生成器"
```

**品牌口号参考**："Upload. Click. Publish."  
或中文版："上传数据，点击运行，直接投稿。"

**核心卖点（Unique Selling Points）**：
1. **投稿级输出**——不需要二次编辑的表格和图表
2. **智能解读**——自动生成论文"结果"章节草稿
3. **中文 first**——编码检测、中文 UI、中文论文格式
4. **一键复现**——参数配置的 JSON 持久化和加载

### 5.4 不应尝试的方向

- 不做 R/Python 通用 IDE（那是 RStudio 的市场）
- 不做贝叶斯统计的重度支持（那是 JASP 的领地）
- 不做完整的数据清洗工具（pandas/Excel 已足够）
- 不做实时协作（技术复杂度远高于收益）
- 不做移动端（回归分析不适合触摸界面）

---

## 附录 A：推荐技术栈

| 层级 | 推荐技术 | 备注 |
|------|---------|------|
| 后端统计引擎 | Python 3.11+ / statsmodels / pandas / numpy | 成熟且精确 |
| Web 框架 | FastAPI (REST) 或 Streamlit (MVP 快速) | API 优先便于前端分离 |
| 前端 UI | React + TypeScript + Ant Design | 组件丰富、中文友好 |
| 图表 | Plotly.js (交互) + matplotlib (导出高 DPI) | 双引擎策略 |
| 桌面封装（可选） | Electron 或 Tauri | Tauri 更轻量但需要 Rust |
| 报告生成 | Quarto (Python) 或 markdown-it + puppeteer | 学术出版级质量 |
| CI/CD | GitHub Actions + pytest + ruff + vitest | 质量门禁 |

## 附录 B：评审总结

| 维度 | 评分 (1-5) | 评语 |
|------|-----------|------|
| 技术可行性 | 5 | 无不可逾越的技术障碍 |
| 市场需求 | 4 | 目标明确，差异化空间清晰 |
| 竞争压力 | 3 | 免费工具多，但细分定位未被满足 |
| 交付风险 | 3 | 范围管理是最大挑战 |
| 团队能力假设 | 3 | 需要统计 + 前端 + UX 复合能力 |
| 总体 | 3.5/5 | 可行，建议进入 POC 阶段验证核心假设 |

--- *本报告由评审 Agent 自动生成，基于项目启动阶段信息，建议每阶段结束后更新。*
