name: Deploy

on:
  push:
    branches: [master]
    paths:
      - 'web/**'
      - 'src/**'
      - 'app/**'
      - '.streamlit/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'Dockerfile'
      - '.github/workflows/deploy.yml'

jobs:
  # -----------------------------------------------------------------------
  # GitHub Pages — deploy web/ to qhWangAntoneva.github.io
  #
  # Uses SSH deploy key (DEPLOY_KEY secret) for cross-repo push access.
  # Setup (one-time):
  #   ssh-keygen -t rsa -b 4096 -f gh-pages-deploy -N ""
  #   gh repo deploy-key add gh-pages-deploy.pub \
  #     -R qhWangAntoneva/qhWangAntoneva.github.io --allow-write \
  #     -t "Regression Analysis deploy key"
  #   gh secret set DEPLOY_KEY -R qhWangAntoneva/Regression-Analysis \
  #     < gh-pages-deploy
  # -----------------------------------------------------------------------
  deploy-pages:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          deploy_key: ${{ secrets.DEPLOY_KEY }}
          external_repository: qhWangAntoneva/qhWangAntoneva.github.io
          publish_dir: ./web
          destination_dir: regression-analysis
          publish_branch: master
          keep_files: true
          commit_message: "deploy: regression-analysis ${{ github.sha }}"

  # -----------------------------------------------------------------------
  # Docker — build image and push to ghcr.io
  # -----------------------------------------------------------------------
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,prefix=sha-
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/master' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
