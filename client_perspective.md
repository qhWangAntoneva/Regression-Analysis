# Client Perspective: Regression Analysis Research Tool

> **Author:** Client / Product Owner Representative
> **Date:** 2026-05-25
> **Purpose:** Define requirements, priorities, and user needs for the Regression Analysis research tool from the demand side.

---

## 1. Core User Stories

The following user stories cover the end-to-end research workflow, from raw data ingestion to publishable output.

### Data Upload & Management

1. 作为一名社会科学研究生，我想要上传 CSV 和 Excel 格式的表格数据集，以便使用我已有的调查数据或公开数据集进行分析，无需手动重新输入数据。
2. 作为一名市场研究员，我想要在上传后预览数据集的前 100 行并查看变量元数据（类型、缺失值比例、唯一值数量），以便快速判断数据质量并决定是否需要清洗。

### Variable Selection & Transformation

3. 作为一名政策分析师，我想要通过下拉菜单和拖拽方式选择因变量、自变量和控制变量，以便直观地构建回归模型，无需编写公式代码。
4. 作为一名社会科学研究生，我想要对变量进行基本转换（取对数、标准化、生成平方项/交互项），以便在线性回归中满足正态性假设或检验调节效应。

### Model Specification & Execution

5. 作为一名计量经济学研究者，我想要配置回归选项（常数项强制包含/排除、标准误类型、置信区间水平），以便在不离开界面的前提下控制模型规格。
6. 作为一名应用统计课程讲师，我想要对比同一因变量下的多个候选模型（逐步添加控制变量、替换核心解释变量），以便在课堂上展示模型稳健性和变量贡献的变化。

### Result Interpretation & Diagnostics

7. 作为一名市场研究员，我想要查看格式化的回归结果表（系数、标准误、p 值、显著性标记、R平方、调整 R平方、F 统计量），以便直接用于报告草稿或讨论。
8. 作为一名政策分析师，我想要一键生成残差图、Q-Q 图、异方差检验和 VIF 多重共线性诊断，以便检验模型是否违反经典线性回归假设。

### Export & Reporting

9. 作为一名社会科学研究生，我想要将回归结果表导出为 LaTeX 格式，以便直接粘贴到学术论文的实证结果章节。
10. 作为一名所有用户，我想要将所有分析结果（模型摘要、诊断图表、描述性统计）打包导出为 PDF 报告，以便与导师、客户或同行评审共享完整的分析流程。

### Interaction & Usability

11. 作为一名不擅长编程的初学者用户，我想要每一步都有清晰的界面提示和错误信息（如"因变量必须是连续变量"），以便在不阅读外部文档的情况下正确完成分析。
12. 作为一名高级用户，我想要查看模型运行的完整日志或底层 R/Python 代码，以便调试异常结果或复现分析。

---

## 2. Feature Priorities (MoSCoW)

### Must Have — 无此功能则产品不可用

| 优先级 | 功能 | 理由 |
|--------|------|------|
| M | 上传 CSV/Excel 数据集 | 整个工作流的入口，没有数据导入则一切无从谈起 |
| M | 变量选择界面（因变量 + 自变量） | 构建回归模型的核心交互方式 |
| M | 普通最小二乘法（OLS）回归引擎 | 社会科学中最基础的回归方法，覆盖 80% 的使用场景 |
| M | 格式化结果表输出（系数、标准误、p 值、R-squared） | 用户运行回归的核心产出，必须有清晰的结构化呈现 |
| M | 描述性统计摘要（均值、标准差、极值、缺失值） | 回归前的标准数据审查步骤，帮助用户理解样本特征 |
| M | 导出回归结果为 CSV/Excel | 用户需要将结果挪到 Word/LaTeX 中进一步编辑 |
| M | 中文界面 | 目标用户群以中文使用者为主，英文界面会显著增加认知负担 |
| M | 基础数据预览（变量名、类型、前 100 行） | 用户必须在上传后立即确认数据是否正确加载 |

### Should Have — 重要但有变通方案

| 优先级 | 功能 | 变通方案 |
|--------|------|----------|
| S | 变量转换（对数、标准化、平方项） | 用户可用外部工具（SPSS / Excel）预处理后再上传 |
| S | 残差图与 Q-Q 图 | 用户可将结果导出到其他统计软件绘图 |
| S | 多重共线性诊断（VIF） | 用户自行计算 VIF = 1/(1-R^2) |
| S | 一键导出 PDF 分析报告 | 用户可逐张截图 + 手动拼报告 |
| S | 控制变量选择与模型对比 | 用户分多次运行，手动记录对比 |
| S | 交互项自动生成 | 用户通过变量转换自行相乘后作为新变量使用 |

### Could Have — 锦上添花

| 优先级 | 功能 | 说明 |
|--------|------|------|
| C | LaTeX 表格导出 | 主要服务经济学/政治学论文写作需求 |
| C | 稳健标准误（HC0-HC3）选项 | 提升结果可靠性，但 OLS 默认标准误已可接受 |
| C | 加权最小二乘法（WLS） | 处理异方差的进阶功能，场景相对有限 |
| C | 复制分析日志（Show underlying R/Python code） | 提升可复现性，但非大多数用户的核心需求 |
| C | 分组合并回归（Group-by regression / Fama-MacBeth 风格） | 面板数据用户的进阶需求 |
| C | 变量标签与值标签管理 | 处理 SPSS 导入数据时的友好性需求 |

### Won't Have — 本次明确不做

| 优先级 | 功能 | 排除理由 |
|--------|------|----------|
| W | 逻辑回归 / Probit / 多项 Logit | 大幅增加模型引擎复杂度；本项目第一阶段聚焦连续因变量的线性回归 |
| W | 面板数据固定/随机效应模型 | 需要面板数据结构识别与 Hausman 检验，架构复杂，不属于 MVP |
| W | 时间序列分析（ARIMA / GARCH） | 完全不同的统计范式，建议独立项目 |
| W | 结构方程模型（SEM） | 需要路径图绘制等重型功能，超出本工具定位 |
| W | 机器学习集成方法（Random Forest / XGBoost） | 本工具定位是经典的推断式回归，不追求预测性能 |
| W | 多用户协作 / 云端存储 | 初期为本地单机工具，SaaS 化留待后续版本 |
| W | REST API 对外暴露 | 产品初期不需要被其他系统调用 |
| W | 直接连接数据库（SQL / NoSQL） | 增加大量适配工作；MVP 阶段仅支持文件上传 |

---

## 3. Target User Personas

### Persona 1: 社会科学研究生 — 张薇

- **背景**：社会学硕士二年级，正在撰写毕业论文，使用 CGSS（中国综合社会调查）数据。编程能力为入门水平（曾在本科课程中学过一点 Stata 基础，从未使用过 Python/R）。
- **技术环境**：Windows 笔记本，无编程环境配置经验，主要依赖 GUI 工具。
- **核心痛点**：
  - Stata 正版授权费用高（学校仅实验室有），且许可证到期后无法使用。
  - 手动在 Excel 中整理回归结果耗时且容易出错。
  - 期刊要求提供 LaTeX 表格格式，但不会编写 LaTeX 代码。
  - 不理解统计术语的中英文对应关系（如 heteroskedasticity / 异方差）。
- **使用模式**：每两周做一次批量分析，每次 3-5 个模型。极度依赖结果的可视化和一键导出，希望在 30 分钟内完成从数据上传到获得草稿表格的全流程。
- **期望**：希望工具像 SPSS 一样直观，但更轻量和免费。

### Persona 2: 市场研究员 — 陈志远

- **背景**：互联网公司用户研究团队在岗研究员，五年工作经验。熟练使用 Excel，会用 Python pandas 处理数据，但对统计建模本身不是专家。日常工作包括满意度调研、转化率归因和客户细分。
- **技术环境**：公司配发的 Windows 笔记本，IT 策略限制安装未授权软件。
- **核心痛点**：
  - 经常需要快速跑回归回答业务问题（"哪个因子对 NPS 影响最大？"），但公司未采购 SPSS/Stata 授权。
  - 团队内部分析流程不统一，每人跑完回归截图样式不同，影响汇报效果。
  - 需要将结果呈现给非技术背景的业务方，用图表比统计量更有效。
  - 数据分析经常不是研究终点，而是开始——需要导出清洗后的数据和模型结果供其他工具使用。
- **使用模式**：高频但零散，每周使用 3-5 次，每次 10-20 分钟，主要用于快速假设验证和汇报材料制作。
- **期望**：希望工具产出物标准化（统一格式的表格、一致的图表样式），减少手动调整的时间损耗。

### Persona 3: 政策分析师 — 李明远

- **背景**：省级政策研究机构在岗分析师，经济学硕士背景。熟悉 Stata 和计量经济学（OLS、IV、DID 等方法论），但日常工作环境中 IT 条件有限，个人电脑性能一般。
- **技术环境**：单位提供的台式机（Windows 7 兼容），可能无法安装新软件，偏好绿色免安装版工具。
- **核心痛点**：
  - 数据文件来自不同部门提供的 Excel，数据格式混乱、变量命名不一致，需要大量预处理时间。
  - 需要向非技术背景的领导汇报，必须提供"一看就懂"的图表和结论摘要，而非原始统计量。
  - 工作流程要求可追溯——每一步的操作记录都必须留档备查。
  - 经常需要在同类数据上反复运行相同的模型组合（如"控制年份固定效应、省固定效应、加入交互项"作为标准操作流程）。
- **使用模式**：项目式工作，每季度 1-2 个大型分析项目，每个项目需要运行 10-20 个以上模型。重视稳定性、可复现性和批量处理能力。
- **期望**：希望工具支持模型配方保存（pre-saved specification templates），让重复性工作一键执行。

---

## 4. Non-Functional Requirements

### 4.1 Performance

| 要求 | 规格 |
|------|------|
| 数据规模上限 | 支持最多 10,000 行 x 200 列的数据集上传和分析 |
| 上传时间 | 10MB 以内的 CSV 文件应在 5 秒内完成解析和预览 |
| 模型运行时间 | 含 20 个变量的 OLS 模型应在 3 秒内返回结果 |
| 图表渲染 | 各类诊断图应在 2 秒内渲染完成 |
| 首屏加载 | 应用初始加载不超过 5 秒（从点击到可交互） |
| 内存占用 | 在分析 10,000 行数据集时，峰值内存占用不超过 500MB |

### 4.2 Usability & Accessibility

| 要求 | 规格 |
|------|------|
| 语言 | 完整中文界面，核心术语提供中英文对照 |
| 错误提示 | 所有操作错误和模型警告使用中文自然语言描述，不暴露原始技术栈的异常栈信息 |
| 新手引导 | 首次使用时有 3-5 步的快速引导弹窗 |
| 表单验证 | 变量选择等关键操作必须有前置校验（如：选择的因变量不能是分类变量） |
| 无障碍性 | 界面元素应支持键盘导航（Tab/Enter/方向键），至少满足 WCAG 2.1 Level A |
| 文档 | 内置帮助中心，涵盖常见问题（"为什么我的模型不显著？"、"如何处理缺失值？"等场景） |

### 4.3 Reliability & Robustness

| 要求 | 规格 |
|------|------|
| 输入容错 | 数据集含缺失值时不崩溃，自动提示并建议处理方式 |
| 模型容错 | 遇到完全共线性、奇异矩阵等计算问题时给出中文可读消息，而非技术堆栈跟踪 |
| 会话持久性 | 用户关闭浏览器后重新打开，应恢复上一次的分析状态（已上传数据、已配置的模型） |
| 崩溃恢复 | 自动保存最近一次模型运行结果到临时文件，防止意外关闭丢失全部工作 |

### 4.4 Compatibility

| 要求 | 规格 |
|------|------|
| 浏览器 | 支持最新两个版本的 Chrome、Edge、Firefox |
| 操作系统 | 支持 Windows 10/11（主要目标平台），macOS 12+（次要） |
| 输入格式 | CSV（UTF-8 编码，自动识别 GBK 编码 fallback）、Excel (.xlsx/.xls) |
| 输出格式 | CSV、Excel、PNG（图表）、PDF（报告）、LaTeX（表格） |

### 4.5 Security & Privacy

| 要求 | 规格 |
|------|------|
| 数据本地化 | 所有用户数据仅在浏览器本地处理，不上传任何外部服务器 |
| 无跟踪 | 工具不收集用户分析数据、变量名、数值或结果 |
| 无远程依赖 | 除首次加载外，核心功能不应依赖网络连接 |
| 导出安全 | 导出的文件中不植入水印、追踪像素或隐藏元数据 |

### 4.6 Maintainability

| 要求 | 规格 |
|------|------|
| 架构 | 前端与分析引擎严格解耦，允许多种后端实现（WebAssembly / Python 本地服务） |
| 测试覆盖率 | 核心回归引擎单元测试覆盖率 ≥ 90% |
| 模型可扩展 | 新增回归模型类型（如 Logit、Poisson）不应修改现有代码超过 3 个文件 |
| 日志 | 所有模型运行记录详细日志（含输入参数、使用的数据子集、计算耗时），便于复现与排查 |
